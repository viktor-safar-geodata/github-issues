<Window
    x:Class="ArcGIS.Net.QueryRelatedIssue.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
    xmlns:local="clr-namespace:ArcGIS.Net.QueryRelatedIssue"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Resources>
        <local:MapViewModel x:Key="MapViewModel" />
    </Window.Resources>
    <Grid>
        <esri:MapView Map="{Binding Map, Source={StaticResource MapViewModel}}" />
        <Button
            Width="200"
            Height="50"
            Command="{Binding ReloadRelatedFeaturesCommand, Source={StaticResource MapViewModel}}"
            Content="Reload related features" />
    </Grid>
</Window>